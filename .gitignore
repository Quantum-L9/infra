# Terraform
.terraform/
*.tfstate
*.tfstate.*
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc

# Never commit real var files or issued secrets
*.tfvars
!*.tfvars.example
*.infisical.env

# Keep the provider lock file committed (do NOT ignore .terraform.lock.hcl)
